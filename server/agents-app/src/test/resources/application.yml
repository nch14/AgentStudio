spring:
  datasource:
    url: jdbc:h2:mem:agents-test;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  sql:
    init:
      mode: never

oss:
  url: http://localhost:9000
  public-url: http://localhost:9000
  username: test
  password: test
  region: us-east-1
  bucket: agents-test
  presign-expire-minutes: 10
